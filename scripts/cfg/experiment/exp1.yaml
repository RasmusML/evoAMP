seed: 123

data:
  dataset_path: "./data/processed/amp_8_35.csv"
  n_seqs: 1000

model:
  latent_dim: 100
  hidden_dim: 64

train:
  val_split: 0.1
  epochs: 100
  batch_size: 64
  lr: 0.001

logging:
  use_wandb: True

visualize:
  plot_loss: False