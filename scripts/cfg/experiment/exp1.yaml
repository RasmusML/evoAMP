seed: 123

data:
  dataset_path: "./data/processed/amp_8_35.csv"
  n_seqs: 2

model:
  encoder:
    embedding_dim: 50 #100
    latent_dim: 64 # 64
    hidden_dim: 128 # 128
    #min_kl_weight: 1e-4
    #max_kl_weight: 1e-2
    #rec_weight: 64.0

train:
  val_split: 0
  epochs: 800
  batch_size: 2
  lr: 0.001

logging:
  use_wandb: False

visualize:
  plot_loss: False