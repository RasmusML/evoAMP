seed: 123

data:
  dataset_path: "./data/processed/amp_8_35.csv"
  n_seqs: 100

model:
  hidden_dim: 32
  latent_dim: 16

train:
  epochs: 30
  batch_size: 64
  lr: 0.001

logging:
  use_wandb: False

visualize:
  plot_loss: True